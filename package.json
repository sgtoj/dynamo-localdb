{
  "name": "dynamo-localdb",
  "version": "0.0.1",
  "description": "CLI application to launch local dynamodb instance and provide commands to create, load, reload, and delete tables with or without data.",
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/sgtoj/dynamo-localdb.git"
  },
  "scripts": {
    "compile": "node ./node_modules/typescript/bin/tsc",
    "start": "node ./scripts/start.js",
    "simple-test": "npm run compile && node ./scripts/test.js"
  },
  "author": "Brian Ojeda",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^6.0.55",
    "tslint": "^4.2.0",
    "typescript": "^2.1.4"
  },
  "dependencies": {
    "aws-sdk": "^2.7.20",
    "dynamodb-local": "0.0.12"
  }
}
